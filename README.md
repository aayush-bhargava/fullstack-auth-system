# fullstack-auth-system
Full-stack authentication system using React + TypeScript (frontend) and Spring Boot (backend). Includes JWT authentication, secure password encryption, and PostgreSQL integration.
